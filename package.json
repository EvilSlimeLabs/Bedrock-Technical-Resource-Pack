{
  "name": "bedrock-technical-resource-pack",
  "version": "0.2.12",
  "private": true,
  "description": "Build tooling for the Bedrock Technical Resource Pack: validates the pack and compiles it to a .mcpack.",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node tools/check.js",
    "audit": "node tools/audit.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "node tools/build.js",
    "release": "npm run check && npm run audit && npm test && npm run build",
    "set-version": "node tools/set-version.js"
  }
}
